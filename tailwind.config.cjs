const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      colors: {
        grayBlue: "#a8b2d1",
        coolTurquoise: "#00d1b2",
      },
    },
  },

  plugins: [],
};

module.exports = config;
